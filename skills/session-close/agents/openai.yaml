interface:
  display_name: "Session Close"
  short_description: "Save handoff and reproduction record"
  default_prompt: "Use $session-close to save a verified HANDOVER.md and REPRO.md, then queue retrospective and AGENTS.md improvements for approval."

policy:
  allow_implicit_invocation: false
