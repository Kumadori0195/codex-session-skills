interface:
  display_name: "Session Close"
  short_description: "Save an adaptive, verified session handoff"
  default_prompt: "Use $session-close to save a verified handoff and only the close artifacts supported by current-session evidence."

policy:
  allow_implicit_invocation: false
