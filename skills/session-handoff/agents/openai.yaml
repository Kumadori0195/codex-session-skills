interface:
  display_name: "Session Handoff"
  short_description: "Capture verified state and reproduction steps"
  default_prompt: "Use $session-handoff to capture the verified state, next action, and reproduction steps for this session."
policy:
  allow_implicit_invocation: false
