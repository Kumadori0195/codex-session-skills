interface:
  display_name: "Session Handoff"
  short_description: "Capture verified state for the next session"
  default_prompt: "Use $session-handoff to capture the verified state and next action for this session."
policy:
  allow_implicit_invocation: false
